services:
  - type: web
    name: pokemon-card-monitor
    env: node
    plan: free
    buildCommand: npm install && node seed.js
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
